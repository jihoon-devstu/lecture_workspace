<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sinse.chatroomapp.mapper.MemberMapper">

    <resultMap id="memberResultMap" type="Member">
        <id property="member_id" column="member_id" />
        <result property="id" column="id" />
        <result property="password" column="password" />
        <result property="name" column="name" />
        <result property="email" column="email" />
        <result property="regdate" column="regdate" />
    </resultMap>

    <select id="selectAll" resultMap="memberResultMap">
        select * from member
    </select>

    <select id="selectById" parameterType="int" resultMap="memberResultMap">
        select * from member where member_id = #{member_id}
    </select>

    <insert id="insert" parameterType="Member">
        insert into member(id,password,name,email) values(#{id},#{password},#{name},#{email})
    </insert>

    <update id="update" parameterType="Member">
        update member
        set id=#{id},
        password=#{password},
        name=#{name},
        email=#{email}
        where member_id = #{member_id}
    </update>

    <delete id="delete" parameterType="int">
        delete from member where member_id = #{member_id}
    </delete>

    <select id="login" resultMap="memberResultMap" parameterType="Member">
        select * from member where id = #{id} and password = #{password}
    </select>

</mapper>